# ─── Sensitive ────────────────────────────────────────────
# IBKR Flex token + Query ID
config.json
sectors.json

# Raw account data pulled from IBKR
data/
ibkr-data/

# Backup of real data taken before generating demo data — NEVER commit (real holdings)
_screenshot_backup/

# Local-only demo-data generator (used to make the README screenshots) — not committed
gen_dummy_data.py

# Built dashboard with real numbers
site/data/

# Logs
logs/
*.log
cron.log
daily.log

# Local screenshots / notes (may contain account IDs etc.)
*.png
*.jpg
*.jpeg
*.heic
NOTES.md
notes.md

# ...but DO commit the README screenshots — these are 100% synthetic demo data,
# so there is nothing private to leak.
!docs/screenshots/
!docs/screenshots/*.png

# ─── Runtime / OS ─────────────────────────────────────────
.serve.pid
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.DS_Store
.idea/
.vscode/
*.swp
